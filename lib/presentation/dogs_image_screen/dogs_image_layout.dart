import 'package:flutter/material.dart';

class DogsImagesLayout extends StatelessWidget {
  const DogsImagesLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: const Scaffold(
        body: Text("DogsImagesScreen"),
      ),
    );
  }
}
